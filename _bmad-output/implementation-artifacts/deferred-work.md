# Deferred Work

Items surfaced during code reviews / development that are real but out of scope for the originating story. Each entry lists the source review and the specific finding so a future story owner can pick it up.

---

## Deferred from: Item 0 verification (Epic 1 retro, 2026-05-03)

- **[RESOLVED by Story 2.1] scaffold-smoke fails locally with "The server does not support SSL connections" — SSL drift bug at heartbeat.js** [worker/src/jobs/heartbeat.js:8-12] — Resolved: `buildPgPoolConfig(connectionString, caCert)` helper in `shared/db/service-role-client.js` now omits SSL for `localhost`/`127.0.0.1`. All three inline pools (health.js, heartbeat.js, founder-admin-only.js) absorbed into the SSoT module. scaffold-smoke expected to pass after this story's PR merges. Resolved 2026-05-03.

---

## Pre-customer-#1 operational gates

Cross-cutting hardening items that must land BEFORE the first paying customer onboards, but do NOT belong to any single feature story. Each item is a standalone chore PR; group into a "Go-Live readiness" mini-epic when the launch window approaches.

- **CSRF protection not registered on any state-changing form route** — `@fastify/csrf-protection` is in `package.json` from Story 1.1 but never `fastify.register`-ed. `POST /signup`, `POST /login`, `POST /forgot-password`, `POST /reset-password` accept form bodies with no CSRF token. SameSite=Lax provides baseline; standalone chore before Go-Live, not retrofit into Story 1.4. Surfaced by code review of Story 1.4 (2026-05-02). [app/src/server.js — registration absent]

---

## Deferred from: code review of 1-1-scaffold-project-two-service-coolify-deploy-composed-health (2026-05-01)

- **[RESOLVED by Story 2.1] pg Pools never `.end()`ed; no SIGTERM handler in app or worker** — Resolved: `shared/db/service-role-client.js` exports `closeServiceRolePool()`. `worker/src/index.js` now has SIGTERM/SIGINT handlers calling it. `app/src/server.js` registers Fastify `onClose` hook. All three inline Pool instances absorbed. Resolved 2026-05-03.
- **/health endpoint not behind `@fastify/rate-limit`** — Plugin is a runtime dep at AC#1 but spec does not register it at Story 1.1. A hostile burst on `/health` could exhaust the `max: 2` pool. Harden in a later security-pass story (Epic 11/12 ops or a dedicated edge-hardening story).
- **`@fastify/cookie` and `@fastify/csrf-protection` not registered** — Listed as runtime deps for AC#1, registration intentionally later. Owner: auth/session story (Story 1.5 founder-admins or onboarding stories that introduce sessions).
- **`MASTER_KEY_BASE64` shape (32-byte base64) not validated** — `shared/config/runtime-env.js` only checks presence, not decodability or 32-byte length. Story 1.2 explicitly owns this per Story 1.1 spec line 401: "Story 1.2 extends this module to add 32-byte length validation on `MASTER_KEY_BASE64`."
- **`.env.example` lists Stripe/Resend keys not in `REQUIRED_VARS`** — `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `RESEND_API_KEY` appear in `.env.example` but `getEnv()`'s required list is only `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_DATABASE_URL`, `MASTER_KEY_BASE64`. Intentional — they're needed at SDK init time only. Tighten in the story that first integrates Stripe (Story 11.1) or Resend (Story 12.2).
- **RLS comment in migration is unverified claim** — `db/migrations/202604301212_create_worker_heartbeats.sql` lines 7-10 claim Supabase's auto-RLS-trigger handles security on `worker_heartbeats`. Verify via Supabase project settings (Studio → Database → Tables → RLS toggle) rather than in code. Owner: any story that audits Supabase project settings (Epic 2 RLS work is a natural fit).
- **No `db:migrate` script in `package.json`** — Smoke test assumes the migration is already applied; nothing in scripts runs it. Pedro currently runs `npx supabase db push` manually per Task 3. Add a `"db:migrate": "npx supabase db push"` (or equivalent) when the team standardizes migration workflow — likely Story 4.1 when more migrations land.
- **Module load order — `health.js` constructs Pool before `getEnv()` validates env** — `app/src/server.js:6` imports `healthRoutes` which evaluates `health.js` and constructs the `Pool` *before* line 8's `getEnv()` runs. Low impact in practice (a Pool with `undefined` connectionString sits idle and `getEnv()` exits before `fastify.listen`), but worth tracking for the day env-validation moves earlier in the boot sequence (e.g., when `shared/db/service-role-client.js` becomes a factory in Story 2.1).

---

## Deferred from: code review of 1-2-envelope-encryption-module-master-key-loader-secret-scanning-hook (2026-05-01)

- **base64 trim asymmetry under non-documented openssl wrapping** — `shared/crypto/master-key-loader.js:45` round-trip uses `raw.trim()` which strips only leading/trailing whitespace; `re-encode` never has interior whitespace. Theoretical concern for very long base64 (>76 chars where openssl wraps); 32-byte keys produce 44 chars, so never triggered by the documented `openssl rand -base64 32`. Defense-in-depth fix would be to strip all whitespace before decoding.
- **secret-scanner pattern coverage is narrow (5 patterns)** — only catches MASTER_KEY-style assignment, shop_api_key assignment, sk_live_, sk_test_, Authorization Bearer. Real Stripe/GitHub leak shapes (restricted-scope keys with dots, scoped tokens) and Supabase service-role JWTs (`eyJ...`) are not covered. Spec-mandated 5 patterns at MVP; broaden in a later security pass (Epic 11/12 ops or pre-launch hardening). `.env.example` enumerates the gap (`SUPABASE_SERVICE_ROLE_KEY`, `STRIPE_WEBHOOK_SECRET` whsec_, `RESEND_API_KEY` re_, `SUPABASE_SERVICE_ROLE_DATABASE_URL` postgres://).
- **single opaque `KeyVaultDecryptError` masks programmer-error vs tamper** — `shared/crypto/envelope.js:82-90` validation branch and `:99-102` crypto-failure branch both throw `KEY_VAULT_DECRYPT_FAILED`. Spec-mandated for attacker-oracle protection but conflates programmer error with security event in metrics/alerts. If alerting on decrypt failures becomes important, split classes.
- **master key buffer not zeroed on shutdown; no `inspect`/`toString` guard** — `worker/src/index.js:15` declares `let masterKey;` at module scope; no `process.on('exit', () => masterKey.fill(0))`. Buffer persists in heap dumps. Also no override of `inspect`/`toString` to prevent accidental debug-log dump. Defense-in-depth beyond MVP.
- **returned EncryptedKey buffers are by-reference** — `shared/crypto/envelope.js:60-65` returns the live `nonce`/`ciphertext`/`authTag` Buffers. Caller mutation between encrypt and persist silently corrupts vault state. Low priority; freeze or copy in a hardening pass.
- **`loadMasterKey` trailing-whitespace edge cases** — `shared/crypto/master-key-loader.js:32,45` mixes `raw` and `raw.trim()`; correct today but fragile. Tighten in a future refactor.
- **`updated_at` has DEFAULT but no auto-update trigger** — `supabase/migrations/202604301204_create_shop_api_key_vault.sql:31` requires every writer to remember `SET updated_at = NOW()`. Add a `BEFORE UPDATE` trigger when the second writer ships (Story 4.3).
- **multi-line secret values bypass line-by-line scanner** — JS template literals or bash heredocs that split a secret across lines defeat all 5 patterns. Inherent to diff-line scanning. Document the limitation in `scripts/check-no-secrets.sh` header.
- **RLS policy silent-fail on `auth.uid()` NULL** — `supabase/migrations/202604301204_create_shop_api_key_vault.sql:42-44` returns 0 rows for anon (correct, but by SQL NULL semantics, not explicit). Add `auth.uid() IS NOT NULL AND ...` defensive clause when Story 2.2's RLS regression suite lands. (Forward-pointer comment already in the migration.)
- **`last_failure_status smallint` accepts negatives and >599** — `supabase/migrations/202604301204_create_shop_api_key_vault.sql:29` no CHECK constraint. Add `CHECK (last_failure_status BETWEEN 100 AND 599)` if the column is meant to be HTTP-only. No analytics depend on it yet.
- **`masterKey` variable is module-scope dead weight at boot** — `worker/src/index.js:15-25` loads and logs but never passes the buffer to any consumer. Story 7.x must thread it through `startHeartbeat` (or successor jobs) — current shape is an architectural placeholder, not a runtime bug.
- **over-padded base64 round-trip accepted; saved only by the 32-byte length gate** — `shared/crypto/master-key-loader.js:44-47` strips trailing `=+` from both sides before comparing, so over-padded inputs slip past the round-trip but fail the `decoded.length !== 32` check. Acceptable today; would silently accept malformed input if AES-128 ever became an option.
- **`withEnv` test helper not robust under concurrent runners** — `tests/shared/crypto/master-key-loader.test.js:7-17` uses `process.env` (process-wide) — fine while `node:test` runs sequentially, breaks if `--concurrency=N` is ever set. Use a stub-env-via-Reflect-defineProperty pattern when test parallelism matters.
- **`.gitattributes` doesn't auto-renormalize pre-existing CRLF working trees** — `.gitattributes:4-5` enforces LF for fresh clones, but Pedro's existing checkout (made before this commit) may still have CRLF in `.githooks/pre-commit`. If Git Bash chokes with `bad interpreter: /usr/bin/env\r`, run `git add --renormalize . && git commit -m "renormalize line endings"`. Document in README if it bites.
- **hook bypassed via GitHub web UI / Desktop GUI / `git commit --no-verify`** — `.githooks/pre-commit:4` is local-only. Pedro must enable GitHub repo Settings → Code security → Secret scanning as the second layer (mentioned in README). The hook is the FIRST line of defense, not the only one.
- **coverage gap on extreme plaintext sizes in envelope tests** — `tests/shared/crypto/envelope.test.js` covers happy-path-size strings only; no `plaintext.length === 1` or `>1 MB`. Code is correct (GCM `update` invariant); coverage gap only. Tighten in a future test-pass story.
- **hook does not fire on `git rebase --continue` / `cherry-pick` / `merge`** — `.githooks/pre-commit` only runs on `git commit` and `git commit --amend`. Secrets introduced via cherry-pick from a feature branch escape local scanning entirely. Document in `scripts/rotate-master-key.md` (or a developer ops doc) that rebase/cherry-pick paths require a manual `bash scripts/check-no-secrets.sh` before push.
- **pathspec `:!*.md` exclusion is git-version-dependent** — `scripts/check-no-secrets.sh:42` works on git ≥2.13 (current Git for Windows is fine); old CI runners with git <2.13 would treat `*.md` as top-level only. Pin git version in CI or use `:(exclude,glob)*.md` explicit form when CI is set up.
- **back-port the two regex transcription-bug fixes to source distillate** — `_bmad-output/planning-artifacts/epics-distillate/01-epics-1-3-foundation-tenancy-mirakl.md` AC#4 still has the broken `MASTER_KEY[A-Z_]*` (no digits) and `Authorization:[ \t]*Bearer` (no optional quote) regex text. Future re-shardings will inherit the bugs. Update both lines to match the corrected forms in `scripts/check-no-secrets.sh`. Housekeeping; outside Story 1.2's commit scope.
- **worker has no SIGTERM handler; masterKey not zeroed on shutdown** — also covered by Story 1.1's deferred "pg Pools never `.end()`ed; no SIGTERM handler" item. Bundle into the same shutdown-discipline story (likely 2.1 or a dedicated ops story).

---

## Deferred from: code review of 1-3-pino-structured-logging-with-redaction-list (2026-05-02)

- **`*["set-cookie"]` wildcard path-string syntax pins to current pino version** — `shared/logger.js:63` emits `*["set-cookie"]` (no dot between `*` and `[`) for the one-level-nested redaction of hyphenated keys. The form is undocumented quirky pino syntax; verified working today with pino@10.3.1 (the parameterized nested-redaction tests pass for `set-cookie`). A future pino major could tighten the parser. The Story 1.3 review patch (add an explicit test for `req.headers["set-cookie"]`) provides early-warning coverage. Action: revisit on next pino major upgrade.
- **`LOG_LEVEL` resolved at module-import time, not runtime** — `shared/logger.js:81` does `const LOG_LEVEL = process.env.LOG_LEVEL || 'info'` once at import. Mutations to `process.env.LOG_LEVEL` after import (test setup, late dotenv load) are ignored. Production flow uses `node --env-file=.env.local` which sets vars before any import, so the realistic failure window is narrow. Defer until a real test-harness or runtime-flip use case appears.
- **`process.exit(1)` paths race async pino flush** — `shared/config/runtime-env.js:21` and `worker/src/index.js:24` call `log.error(...)` then `process.exit(1)` without `logger.flush()` or `pino({ sync: true })`. pino's default async mode buffers via sonic-boom; a startup misconfig could exit before the error line flushes — the worst possible debugging UX for the very thing these exit paths exist to surface. Pre-existing pattern from Story 1.1 (Story 1.3 only changed the construction site). Warrants a coordinated cross-cutting fix (sync-mode logger OR `pino.final` pattern OR explicit flush) across all log-then-exit sites.
- **ESLint `no-restricted-syntax` selector misses bracket-indexed and aliased `process.stdout.write`** — `eslint.config.js:29-30` matches `callee.object.object.name="process"` literally, so `process['stdout'].write(...)` (computed property) and `const o = process.stdout; o.write(...)` (alias) escape detection. Guardrail for accidental regressions, not a security boundary — the runtime redaction list is the actual guarantee. Fixing requires a more elaborate AST pattern or a custom rule (Story 3.1+ ships custom rules); not justified at MVP scale.

---

## Deferred from: code review of 1-4-signup-endpoint-atomic-profile-trigger-source-context-capture (2026-05-02)

- **`mp_session` cookie stores access + refresh tokens as plaintext signed JSON (not encrypted)** — Spec-sanctioned design (Task 8 + Story 2.1 contract): `@fastify/cookie` "signed" cookies are HMAC-authenticated, not encrypted. Refresh token (long-lived bearer credential) sits in plaintext under the signature. Future hardening: server-side opaque session ID with a Redis/DB-backed session store, OR envelope-encrypt the cookie value with `MASTER_KEY_BASE64` (Story 1.2 SSoT). Owner: Story 2.1 (RLS-aware DB client) is the natural place since it's the cookie reader. [app/src/routes/_public/login.js:64-78]
- **`mapSignupError` HINT-substring path is dead per the route's own comment** — GoTrue empirically strips Postgres trigger HINTs before the JS-side `AuthError` is shaped; `signup.js:50-58` acknowledges this and adds route-level pre-validation as the real path. The HINT logic in the mapper is kept as defense-in-depth (covers direct-DB writes / future code paths that bypass the route), but is a no-op in the user-facing flow. Document the dual-path explicitly when the next signup-touching story lands; consider promoting the route pre-validation into a shared `shared/validation/profile-trim.js` if any other entrypoint introduces customer-profile writes. [app/src/lib/signup-error-mapper.js:31-35]
- **`secure: NODE_ENV === 'production'` cookie flag may emit Insecure cookies on staging** — Three setCookie sites (`server.js` parseOptions, `source-context-capture.js`, `login.js`) gate Secure on a literal `=== 'production'` check. Once a staging environment is defined, decide whether `NODE_ENV` is `'production'` there too or whether the gate becomes "any environment behind TLS" (e.g., `process.env.FORCE_COOKIE_SECURE` opt-in). Owner: ops/deployment story near launch. [app/src/server.js + app/src/middleware/source-context-capture.js + app/src/routes/_public/login.js]
- **`?msg=password_reset_ok` banner enables phishing-grade UX spoofing** — Attacker hosts a redirector that lands users on `/login?msg=password_reset_ok` and shows the fake "password reset OK" banner, then phishes credentials from the legitimate login form. Mitigation options: (a) sign the msg via signed-cookie flash (consume-once); (b) replace the query-driven banner with a server-side flash via the `mp_source_ctx`-style cookie pattern; (c) accept the surface and add a "if you didn't request this, ignore" line. Owner: any UX-pass story or Epic 8 dashboard chrome. [app/src/routes/_public/login.js + app/src/views/pages/login.eta]
- **No length/charset CHECK constraint on `customers.source` / `customers.campaign`** — Validation lives only in the middleware (`source-context-capture.js`); storage is unbounded `text`. Future schema-hardening pass should add `CHECK (length(source) <= 100 AND source ~ '^[A-Za-z0-9_.\\-]+$')` and the same for `campaign`. Pairs with the middleware sanitize tightening (Story 1.4 patch P11). Owner: schema-hardening pass or Epic 4 onboarding when first marketplace-attribution UX lands. [supabase/migrations/202604301200_create_customers.sql]
- **Trigger writes `NEW.email` without case normalization** — `customers.email` may diverge from `auth.users.email` in casing if Supabase changes its normalization. Future story: decide on `CITEXT` for `customers.email` OR `lower(trim(NEW.email))` in the trigger. Cosmetic today (downstream consumers can use `auth.users.email`), but pre-locks pain when Stripe/Moloni linkage starts using `customers.email` directly. Owner: Story 11.x billing or the first ops-tooling story that joins on email. [supabase/migrations/202604301201_create_customer_profiles_with_trigger.sql]
- **Integration test feeds set-cookie back without isolating write-side check** — Test happens to exercise the broken `sourceContextCapture` write-side path (story patch P4) and still passes because the read side is correct. A regression in `unsignCookie` ordering would not be caught. Add a dedicated unit test for the write-side first-write-wins predicate against a tampered (forged) cookie. — test quality nit. [tests/integration/signup-flow.test.js:1717-1722]
- **`mp_session` cookie payload approaches 4KB browser limit when JWT/refresh-token payloads grow with custom claims** — Latent failure mode; not currently triggered (Supabase JWTs ~700 bytes each). If any future story adds custom JWT claims (org metadata, RLS context fields), risk surfaces. Mitigation overlaps with the encrypted-session-store hardening (W1 above). Owner: Story 2.1 or whichever story first adds custom JWT claims. [app/src/routes/_public/login.js]
- **`reset-password.js` extracts tokens from URL fragment only, no query-string fallback** — Supabase recovery flow uses fragment by default, but auth-flow config can return tokens in query string. If Pedro changes the project Auth config, `public/js/reset-password.js` silently no-ops. Add a `URLSearchParams(window.location.search)` fallback if/when the recovery email format changes. [public/js/reset-password.js]
- **Trigger does not strip non-printable / control bytes from name fields** — Submitting `first_name="Pedro\\x07"` (BEL) commits a row with embedded control bytes; downstream Moloni invoicing receives corrupted strings. Partial mitigation: tightening middleware sanitize (Story 1.4 patch P11) prevents the source/campaign route; profile names still need a CHECK constraint or trigger-level cleanup. Owner: schema-hardening pass. [supabase/migrations/202604301201_create_customer_profiles_with_trigger.sql:91-100]
- **`forgot-password` renders generic confirm on schema-validation failure (deliberate hardening, drift from spec's natural reading)** — AC#6 implies HTTP 400 on bad email format; route returns the generic confirmation to prevent distinguishing "valid email, not registered" from "invalid email format". Defensible UX hardening but unauthorized by spec. Document in Change Log; consider amending AC#6 in a story update if other reviewers also ask. [app/src/routes/_public/forgot-password.js:48-56]
- **Signup HTML field-error mapping uses dynamic key assignment (`out[field]`) — theoretical prototype-pollution path** — Requires AJV to emit `__proto__` as a path segment, which the current schema cannot trigger (no `__proto__` in any required/properties). Latent: harden if the schema grows or if a per-field validator added later allows arbitrary key names. Use `Object.create(null)` for `out` or guard against `field === '__proto__' || field === 'constructor'`. [app/src/routes/_public/signup.js:590]
- **Trigger does not strip non-ASCII whitespace from required name fields (P7 carry-over)** — Route-side `.trim()` is Unicode-aware in Node 22+ and rejects NBSP-only input as required. The trigger's `length(trim(...)) = 0` defense-in-depth check, however, uses Postgres `trim()` which only strips ASCII space chars — so a direct DB write or future code path that bypasses the route can commit a `first_name` like `' '` (NBSP). Fix at the trigger requires deviating from the architecture-distillate's verbatim DDL contract (`architecture-distillate/06-database-schema.md#customer_profiles`); should land alongside a schema-hardening pass that also adds CHECK constraints on `customers.source` / `customers.campaign` (W5) and strips control bytes from name fields (W10). Owner: schema-hardening pass or whichever future story first writes to `customer_profiles` outside the route. [supabase/migrations/202604301201_create_customer_profiles_with_trigger.sql:91-104]
- **`getAnonSupabaseClient` reads `process.env` at first-call instead of via `getEnv()`** — Latent test-ordering / stale-env bug; defeats the purpose of the env-validation gate. Tightening this requires deciding the module-init contract (server.js calls `getEnv()` at boot, but `supabase-clients.js` is imported by `_public` route modules which are registered later). Best unified with a broader env-binding refactor when Story 2.1's `shared/db/` clients land — they'll need the same `getEnv()`-via-bag pattern, and addressing both at once avoids two passes. [app/src/lib/supabase-clients.js]

---

## Story 1.5 — admin_access_denied + lookup-error email PII (GDPR Phase 2 trigger)

At MVP, `app/src/middleware/founder-admin-only.js` logs the plaintext attempted email in TWO places: the denial branch (`event_type: 'admin_access_denied', email_attempted: <plaintext>` on info-level — documented in spec AC#3) AND the lookup-error catch branch (`{err, email: <plaintext>}` on error-level — surfaced by Story 1.5 review 2026-05-03). The redaction list in `shared/logger.js` does not currently include either path's email field — admin-denial volume is expected to be ~0 events/year (Pedro is the only founder; mistargeted requests would come from broken bookmarks or scanners), and lookup-error volume is bounded by infrastructure incidents.

Phase 2 trigger conditions (any of):
1. Admin denials exceed 5/year (signal that the field is accumulating non-trivial PII)
2. GDPR audit asks for PII minimization in operational logs
3. Email-redaction list extension lands as a separate hardening pass

Implementation when triggered: ~4-line change in `founder-admin-only.js` — apply the same SHA-256-prefix transform in BOTH the denial branch (`email_attempted: ...`) AND the lookup-error catch branch (`email: ...`). `crypto.createHash('sha256').update(request.user.email).digest('hex').slice(0, 8)` (first 8 hex chars; sufficient for log correlation across denial events for the same email; irreversible — no plaintext recovery from the log line). [app/src/middleware/founder-admin-only.js]

- **[RESOLVED by Story 2.1] `founder-admin-only.js` inline pg.Pool with conditional SSL** — Resolved: absorbed into `shared/db/service-role-client.js`. `getFounderAdminPool()` and `endFounderAdminPool()` preserved as deprecated delegate exports for test teardown compatibility. Resolved 2026-05-03.

---

## Deferred from: code review of 1-5-founder-admins-seed-admin-auth-middleware (2026-05-03)

- **Verify `tests/integration/_helpers/reset-auth-tables.js` env guards** — Story 1.4 helper is reused in Story 1.5 to wipe `auth.users` between tests. If misconfigured to point `SUPABASE_SERVICE_ROLE_DATABASE_URL` at non-local Supabase, the helper would forcibly confirm + delete production users. Should have an explicit guard like `assert(connectionString.includes('localhost') || connectionString.includes('127.0.0.1'))` before any DELETE. Pre-existing from Story 1.4; tighten as part of test-harness hardening pass. [tests/integration/_helpers/reset-auth-tables.js]
- **Add 503-path test exercising DB-unavailable branch in `founder-admin-only.js`** — `app/src/middleware/founder-admin-only.js:97-102` (catch → 503 + `admin-denied.eta` with retry message) is dead code from a coverage perspective. A regression that swaps 503 → 500, removes the view-render, or changes the PT message would not be caught. Add a test that injects a pool-throw (e.g., point at unreachable port via env override or stub `getPool().query`). [tests/integration/admin-middleware.test.js]
- **[RESOLVED by Story 2.1] Production shutdown hook for `endFounderAdminPool`** — Resolved: `app/src/server.js` registers Fastify `onClose` → `closeServiceRolePool()`. All pools share a single SSoT singleton. Resolved 2026-05-03.
- **Layout case-sensitivity verification on Linux deploy** — `app/src/views/pages/admin-denied.eta:1` references `'/layouts/default'`. Windows dev (case-insensitive FS) loads regardless of file-name case; Linux prod containers (case-sensitive ext4) require an exact match against `app/src/views/layouts/default.eta`. Verify on first Coolify deploy that the page renders correctly. Same concern applies to all eta-rendered pages from Story 1.4 onward; not unique to Story 1.5. [app/src/views/pages/admin-denied.eta:1, app/src/views/layouts/default.eta]
- **First-call race on `getPool()` singleton in `founder-admin-only.js`** — Two parallel first-callers race the `if (_pool === null)` check; both create a Pool, one is leaked (never `.end()`'d). Real but unlikely under MVP load (one founder, one admin route once Story 8.10 ships). Story 2.1's `shared/db/service-role-client.js` SSoT will fix centrally with a one-shot init promise. [app/src/middleware/founder-admin-only.js:52-64]
- **Cross-file test isolation: `admin-middleware.test.js` + `signup-flow.test.js` race on shared `auth.users`** — `node --test tests/integration/signup-flow.test.js tests/integration/admin-middleware.test.js` (both files in one invocation) fails `authenticated_founder_proceeds_with_adminContext` because node:test runs files in parallel by default and the two suites' `resetAuthAndCustomers()` calls race on shared `auth.users` rows. Both pass in isolation (admin-middleware: 10/10; signup-flow: 11/11). Fix options: (a) add `--test-concurrency=1` to a future `npm run test:integration` script in package.json, (b) namespace each suite's test emails with a per-suite suffix so resets don't collide. Surfaces only when a developer (or future CI) runs the integration suite as a single command rather than per-file. [tests/integration/admin-middleware.test.js, tests/integration/signup-flow.test.js, package.json scripts]

---

## Deferred from: PR #64 review (2026-05-03)

- **No test for `getRlsAwareClient` invalid-JWT clean-throw** [tests/shared/db/clients.test.js] — expired, malformed, or structurally invalid JWT is not exercised; no assertion that `getRlsAwareClient` throws cleanly without leaking the credential string. Low risk at MVP (JWT comes from Supabase Auth, not raw user input), but the error path through `set_config` + `RESET ROLE` on failure is untested. Add a case in an `.additional.test.js` supplement during Epic 2 hardening.
- **RESET ROLE + `request.jwt.claims` clearance not verified on release** [tests/shared/db/clients.test.js] — `rls_context_middleware_binds_request_db` checks `pool.totalCount === pool.idleCount` after request completion but does not assert that session-level `ROLE` and `request.jwt.claims` are actually reset by the wrapped `release()`. A misconfigured RESET sequence would pass the pool-idle check while silently leaking RLS context to the next checkout. Add a second connection to the same pool post-release and assert `CURRENT_ROLE` and `current_setting('request.jwt.claims')` are back to defaults.
- **`tx()` acquire-failure rollback path untested** [tests/shared/db/clients.test.js] — if `pool.connect()` itself throws (pool exhausted, network drop before BEGIN), the error propagates without touching BEGIN/ROLLBACK — correct behaviour but uncovered. The existing `tx_helper_rolls_back_on_throw` only covers the callback-throw path. Add a pool-stub case.
- **PR body hallucinated env var `SUPABASE_SERVICE_ROLE_KEY`** [PR body] — body describes the service-role pool as using `DATABASE_URL + SUPABASE_SERVICE_ROLE_KEY` (two separate env vars). Actual code uses only `SUPABASE_SERVICE_ROLE_DATABASE_URL` (a single connection string embedding the key). `SUPABASE_SERVICE_ROLE_KEY` does not appear anywhere in the diff. Cosmetic but could mislead a future developer configuring `.env` from the PR description; update the body or add a comment in `.env.example`.
- **PR body file count off by one** [PR body] — body claims "16 files, +1644 / -335 lines"; diff has 15 files. `package.json` is present in the diff but not listed in the body's "Files changed" section. Cosmetic; no runtime impact.

---

## Deferred from: code review of story-2-2-rls-regression-suite-ci-block (2026-05-04)

- **Generic INSERT path in parameterized RLS test only inserts `(${ownerCol})`** [tests/integration/rls-regression.test.js:446-449] — the generic A-cannot-INSERT-as-B branch issues `INSERT INTO ${table} (${ownerCol}) VALUES ($1)` for any table that isn't `customers` or `customer_profiles`. Future Epic 4-11 customer-scoped tables will likely have additional NOT NULL columns; the INSERT will trip `23502 not_null_violation` instead of `42501 insufficient_privilege`. Today this is acceptable because the script's `RLS_BLOCKED_CODES_MUTATION` allow-list accepts both codes AND the test path uses `assert.ok(threw)` (any throw passes). However, this masks the real RLS-vs-constraint distinction. When Epic 4 adds the first real customer-scoped table, refactor the generic path to use a per-table-config `insertRow` shape that supplies all required columns, so a 23502 from a missing required column doesn't get conflated with a real 42501 RLS block.

---

## Deferred from: PR #65 review (2026-05-04)

- **AC-2 partial: only SELECT covers B→A direction** [tests/integration/rls-regression.test.js:427-571] — INSERT/UPDATE/DELETE tests use `jwtA` against `userBId` only; AC-2(b) explicitly says "repeats with customer B's JWT against customer A's rows" for all 4 ops. SELECT covers both directions; INSERT/UPDATE/DELETE only A→B. Mechanical to add (mirror the existing 3 tests with `jwtB` + `userAId`). Defer because RLS predicates are typically symmetric — the asymmetry is a coverage gap, not a known-broken case. Add when Epic 4 extends the suite for the first real `customer_marketplaces`-scoped table.
- **PR body framing claims "GitHub Actions skipped: RUN_CI_LOCALLY=true"** [PR #65 body, "Local CI results" section] — actual GitHub Actions runs were green (Lint pass 14s, RLS Regression Suite pass 1m0s); no `RUN_CI_LOCALLY` flag exists anywhere in the diff. Cosmetic body overstatement matching the known BAD Step 6 hallucination pattern (project memory `feedback_bad_pipeline_trust.md`). Not worth amending the merged commit body; recurring pattern worth noting at next retro.
- **No anon-JWT / NULL `auth.uid()` negative test** [tests/integration/rls-regression.test.js] — the new defensive `auth.uid() IS NOT NULL AND` clause in `shop_api_key_vault_select_own` (added by Task 5 of this story) is not exercised by any test where `auth.uid()` is actually NULL. Currently the suite only tests authenticated cross-tenant isolation. Add a `rls_isolation_anon_returns_zero_rows_for_every_table` test when shop_api_key_vault row-level tests come out of deferred status in Epic 4.

---

## Deferred from: PR #66 review (2026-05-04)

- **PR body data-grounding miscount (cosmetic; documented BAD Step 6 pattern)** [PR #66 body] — PR #66 body claims "12 files changed, 2078 insertions(+), 155 deletions(-)" but actual `git diff main --stat` is 10 files / 2147 insertions / 84 deletions. Body's changelist also lists `.claude/skills/bad/SKILL.md` and `_bmad-output/implementation-artifacts/sprint-status.yaml` which are NOT in the PR diff (sprint-status flips happen on main, not in the worktree branch). Technical substance of the body (file paths of NEW code, AC coverage, env vars, behavioral claims) is fully accurate. Pattern-tracking only — no code change needed. Recurrence of the issue documented in `feedback_bad_pipeline_trust.md`.

---

## Deferred from: PR #67 review (2026-05-06)

- **PR body hallucinations** [PR body] — body claims "17 files changed, 898 insertions, 754 deletions" and lists `sprint-status.yaml` and several `_bmad-output/design-references/` removals/updates (chat2.md, MarketPilot-standalone.html, dashboard-and-audit.html, phase-d.jsx, 13-admin-status.html, 14-key-revoked.html, 15-payment-failed.html) that do not exist in the actual diff. Actual: 9 files, 780 insertions, 2 deletions. Cosmetic BAD Step 6 hallucination pattern; no functional impact.
- **`runMonthlyPartitionCreate` error-logging path not unit-tested** [tests/worker/src/jobs/monthly-partition-create.test.js] — the `logger.error` branch (db.query rejection propagating outside the inner try/catch) is untested due to static-import mocking constraints. Acceptable for MVP; cover when `--experimental-vm-modules` becomes stable or in a `.additional.test.js` supplement when Story 5.1 ships.

---

## Deferred from: code review of story-3.1 (2026-05-06)

- **`res.json()` may throw `SyntaxError` on malformed 2xx response body** [shared/mirakl/api-client.js:113] — caller would receive a non-`MiraklApiError`, breaking the SSoT contract. Wrap in try/catch and rethrow as `MiraklApiError('malformed JSON body', res.status, 'WORTEN_MALFORMED_RESPONSE')`. Deferred — not exercised in MVP, no test coverage; revisit if Worten ever returns malformed JSON or once empirical fixtures exist.
- **`no-direct-fetch` ESLint rule misses computed-property `obj['fetch']()` bypass** [eslint-rules/no-direct-fetch.js:55-66] — rule only matches `Identifier`-named property `fetch`. A computed access like `globalThis['fetch']()` slips through. Deferred — bypass requires deliberate evasion; the rule prevents accidental usage which is the actual goal. Tighten if a code-review audit ever finds a real instance.
- **`no-direct-fetch` ESLint rule misses re-export bypass `export { fetch } from 'node:fetch'`** [eslint-rules/no-direct-fetch.js:75-81] — only `ImportDeclaration` is hooked; `ExportNamedDeclaration` with source is not. Deferred — same reasoning as computed-access.
- **`no-direct-fetch` ESLint rule misses dynamic `await import('node:fetch')`** [eslint-rules/no-direct-fetch.js:75-81] — dynamic imports are `CallExpression` with `callee.type === 'Import'`, not `ImportDeclaration`. Deferred — same reasoning as computed-access.
- **`backoffDelay` does not propagate AbortSignal — worker SIGTERM waits up to 16s for in-flight delay** [shared/mirakl/api-client.js:32-34] — switch to `setTimeout` from `node:timers/promises` with a signal parameter once a worker-wide AbortController exists. Deferred — graceful-shutdown handling is owned by Story 5.1 (master-cron dispatcher) and was outside Story 3.1 scope.
- **Test "transport errors are retried (5xx schedule)" only asserts final error, not retry count** [tests/shared/mirakl/api-client.test.js:169-176] — assertion does not actually verify the 5-retry schedule was followed for transport-level failures. Deferred — would require fake timers (`MockTimers` or `sinon`) or a counting mock server bound to localhost:1; fix when the test scaffold gets a fake-timer pass.
- **Source-file string-sniff test (`for dep of forbidden`) is brittle to comments mentioning forbidden libs** [tests/shared/mirakl/api-client.test.js:97-104] — a future comment in `api-client.js` mentioning `node-fetch` (e.g., "we removed node-fetch") would false-positive. Deferred — fragility known; useful guardrail despite it. Replace with an AST-based check or `package.json` dependency check when budget allows.
- **`audit_log_select_own` RLS policy is `USING (false)` placeholder** [supabase/migrations/202604301208_create_audit_log_partitioned.sql] — story correctly defers the real subquery to Story 4.1 (requires `customer_marketplaces` table). Story 4.1 BAD subagent must `DROP POLICY audit_log_select_own ON audit_log` and `CREATE POLICY` with the real subquery expression.

---

## Deferred from: PR #68 review (2026-05-06)

- **"GitHub Actions skipped" claim is a factual inversion** [PR #68 body] — PR body states "GitHub Actions skipped: RUN_CI_LOCALLY=true" but CI actually ran and passed (green checks). Recurring BAD Step 6 hallucination pattern; third occurrence (also PR #65, PR #66 bodies). Process note only: worth raising at Epic 3 retrospective so BAD's Step 6 grounding patch can be prioritised.
- **File count / stat undercounts in PR body** [PR #68 body] — body claims "7 files changed, 781 insertions(+), 22 deletions(-)" but actual diff is 8 files / 801 additions / 29 deletions. Cosmetic; same class as PR #66 data-grounding miscount. No functional impact.
- **AC coverage claim understates coverage** [PR #68 body] — body states "2 ACs covered" but story has 5 ACs, all covered by the 22-test suite. Cosmetic body understatement; no impact on correctness.
- **Missing `[Skip-Live-Smoke: ...]` marker on a `shared/mirakl/` PR** [PR #68 body] — the Live Smoke Evidence guard triggers for any PR touching `shared/mirakl/`. Story spec documents no live Mirakl endpoint calls (client-only infrastructure with no credential at this stage), so a skip marker with reason `client-only infrastructure, no endpoint calls` would have been appropriate. Process note: future PRs touching `shared/mirakl/` should include this marker to prevent the guard from halting the review.

---

## Deferred from: PR #69 review (2026-05-06)

- **PR body claims sprint-status.yaml updated** [PR body] — `_bmad-output/implementation-artifacts/sprint-status.yaml` listed under "Planning artifact updated" but is not in the PR diff; main already has story 3.2 as `done` (updated via a prior direct coordinator commit). Known BAD Step 6 hallucination pattern — cosmetic only.
- **PR body test count understated: "46 test cases" vs actual 55** [PR body] — BAD wrote the body after Step 6 (before Step 5's additional defensive input validation tests). Not a quality issue.
- **PR body file count wrong: "10 files changed" vs actual 9** [PR body] — off-by-one, minor cosmetic error.
- **No live-smoke marker for a Mirakl-touching PR** [PR body] — Story 3.2 is mock-only by spec design (live credential smoke deferred to Story 3.3). Future Mirakl-touching PRs that are expected to run live should include `[Skip-Live-Smoke: <reason>]` to make the intent explicit.

---

## Deferred from: PR #70 review (2026-05-06)

- **PR body claims sprint-status.yaml as a changed file** [PR body / Changes section] — sprint-status.yaml is not in the diff; Story 3.3's `done` status arrived on main via BAD's local push-race commits. Cosmetic body overstatement per the known BAD Step 6 hallucination pattern. No functional impact.
- **Live credential smoke for Story 3.3 not documented in PR** [PR body] — The prior deferred entry from Story 3.2's review (deferred-work.md line 177) explicitly stated "deferred to Story 3.3." This PR contains no live smoke evidence and no `[Skip-Live-Smoke: <reason>]` marker. Resolve by running `npm run mirakl:verify` against real credentials post-merge and recording the result.

---

## Deferred from: code review of story-4.1 (2026-05-06)

- **`transitionCronState` silently skips audit emission when `tx.query` is not callable** [shared/state/cron-state.js:151] — The conditional `if (eventType !== null && tx != null && typeof tx.query === 'function')` is a deliberate concession for unit tests where `mockTx` is a wrapper function (`async (client, cb) => cb(client)`), not a pg PoolClient. The dev's JSDoc documents this. Risk: a future production caller that forgets to pass `tx` (or passes a wrapper-shaped tx) would silently break Bundle B atomicity for mapped transitions — state changes commit but no audit row is written. Production callers always pass `tx = client` (the pg PoolClient inside a transaction) per the integration test pattern, so this is latent. Tighten by either (a) making the integration test pattern enforced — throw if mapped transition has no callable `tx`; or (b) refactoring unit tests to pass a mock client as `tx`. Defer until a real instance of forgotten `tx` surfaces.
- **`transition_emits_audit_event_for_mapped_transitions` test compares count() to LIMIT-1 length** [tests/shared/state/cron-state.test.js:982-994] — `countBefore` is from `SELECT COUNT(*)::int`; `countAfter` is `afterRows.length` (capped at 1 by LIMIT). The assertion `countAfter - countBefore === 1` only holds when `countBefore === 0`, which is true post-`resetAllCustomers()`. Fragile if seed cleanup is ever skipped. Replace with `SELECT COUNT(*)::int` on both sides for clarity. Pre-existing pattern; not blocking.
- **Stale TODO comment in `202604301204_create_shop_api_key_vault.sql`** [supabase/migrations/202604301204_create_shop_api_key_vault.sql:55-57] — TODO `Story 2.2: add 'shop_api_key_vault' to scripts/rls-regression-suite.js coverage list` is now satisfied by Story 4.1 (vault is in CUSTOMER_SCOPED_TABLES with real isolation tests). Cleanup blocked by migration immutability rule (file was committed pre-Story-4.1; the rename activated it but did not edit content). Address via a follow-up doc-only patch when next story touches `supabase/migrations/`.
- **`fullA01Pc01Columns()` test fixture uses nonsensical channel data** [tests/shared/state/cron-state.test.js:288] — `channels: ['{}']` produces a text[] with one element being the literal string `'{}'`. Acceptable as shape-only fixture but sets an awkward precedent. Replace with `channels: ['electronics']` (or whatever the real Worten channel slugs become) when Story 4.4 adds real PC01 data.

---

## Deferred from: PR #71 review (2026-05-06)

- **`audit_log_select_own` RLS policy replacement not delivered by Story 4.1** [supabase/migrations/202604301208_create_audit_log_partitioned.sql] — Story 4.1 was the explicit owner of this deferred obligation (see `deferred-work.md` line 159: "Story 4.1 BAD subagent must DROP POLICY audit_log_select_own ON audit_log and CREATE POLICY with the real subquery expression"). No new migration was created; policy remains `USING (false)`. `customer_marketplaces` now exists so there is no technical blocker. Carry forward to Story 4.4 (the first post-4.1 migration owner) or a dedicated chore migration before Epic 8 dashboard lands (Epic 8 is the first real consumer of `audit_log` customer reads). New migration must: `DROP POLICY audit_log_select_own ON audit_log; CREATE POLICY audit_log_select_own ON audit_log FOR SELECT USING (auth.uid() IS NOT NULL AND customer_marketplace_id IN (SELECT id FROM customer_marketplaces WHERE customer_id = auth.uid()));`.
- **PR body claims sprint-status.yaml as changed file** [PR #71 body] — `_bmad-output/implementation-artifacts/sprint-status.yaml` listed as file #3 in the PR body changelist; file count inflated to 15 vs actual 14. Insertion/deletion counts also off (2305/106 vs actual 2395/102). Cosmetic BAD Step 6 hallucination pattern (fourth occurrence across PRs #66, #69, #70, #71). No functional impact.
