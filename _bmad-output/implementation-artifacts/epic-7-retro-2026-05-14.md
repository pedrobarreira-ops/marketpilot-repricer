# Epic 7 Retrospective — Engine Decision & Safety

**Date**: 2026-05-14
**Facilitator**: Bob (`bmad-retrospective`)
**Project Lead**: Pedro
**Scope**: Full epic retrospective. Epic 7's 9 stories all shipped (8 substantive + 7.9 cleanup chore); production-correctness for the Bundle C participants was closed at the [Bundle C close-out retro 2026-05-13](bundle-c-close-out-retro-2026-05-13.md); this retro picks up the post-close-out work (Stories 7.4, 7.5, 7.7) plus systematic follow-through on the 17 close-out action/watch items.
**Stories Reviewed**:
- 7.1 (`shared/money` + `no-float-price` ESLint, calendar-early)
- 7.2 + 7.3 + 7.6 + 7.8 (Bundle C engine + cooperative-absorb + circuit-breaker + atomicity gate — mega-merged via PR #91 squash `89b2378` 2026-05-11)
- 7.9 (Bundle C cleanup chore — PR #93 2026-05-13)
- 7.4 (anomaly-freeze SSoT + accept/reject endpoints — PR #94 2026-05-13)
- 7.5 (tier-classify SSoT + decide.js wire-up — PR #95 2026-05-13)
- 7.7 (reconciliation cron — PR #96 2026-05-14, LAST Epic 7 story)
**Context-budget audit**: ✅ clean — `node scripts/context-budget.js` reports TOTAL 34.5k tokens, all files within threshold. No file flagged OVER; `bad/SKILL.md` 968/1000, `bad-review/SKILL.md` 678/750, `project-context.md` 583/600, `epics-distillate/_index.md` 452/500 all in the "near" band. No retro action item from budget audit.
**Sprint-status update**: `epic-7-retrospective: optional → done` (this retro doc); `epic-7: in-progress → done` (all 9 stories complete + close-out and post-close-out verdicts logged).

---

## Why this retro now

Epic 7 closed in three distinct phases: the calendar-early Story 7.1 (2026-05-10), the Bundle C mega-merge (2026-05-11) whose joint-correctness verdict was already closed at the [Bundle C close-out retro 2026-05-13](bundle-c-close-out-retro-2026-05-13.md), and the three post-close-out stories (7.4, 7.5, 7.7) plus the 7.9 cleanup chore that shipped 2026-05-13/14. The post-close-out batch is where the load-bearing learnings of this retro live: a 4-sighting "spec-outcome-without-mechanism" pattern matured into a templatable discipline (Section A), a Bundle B atomicity contradiction got ratified into the doc-vs-reality decision deferred at close-out (Section C), and a 3-sighting push-race got promoted from operational quirk to a fixable failure mode (Section F1). The retro also runs systematic close-out follow-through on Q1–Q9 + W1–W8 (Section L) and validates two BAD-pipeline patches that landed mid-batch (Section E).

---

## A. Spec-discipline maturity

### A1. 4-sighting spec-outcome-without-mechanism pattern → memory promotion

**Pattern**: spec describes a desired OUTCOME but is silent or wrong on the MECHANISM by which that outcome is achieved. Dev follows the literal mechanism that doesn't exist, OR rationalises a divergence to meet the outcome; the gap surfaces at /bad-review or live-smoke.

**Sightings (chronological)**:
1. **Story 6.3 wire-up gap** (2026-05-09) — spec described `scheduleRebuildForFailedSkus` outcome; mechanism (upstream call site in `pri02-poller.js`) was missing → dead code, caught by `/bad-review`'s `code-vs-spec` subagent.
2. **Story 7.8 fake-gate** (2026-05-11) — spec required integration tests against real Bundle C modules; mechanism was missing because story-7.8 worktree forked from story-7.6 tip and never had cooperative-absorb.js → tests passed against a stub-fallback, caught by Bob's audit § 5.
3. **Story 7.4 AC1 in-tx alert** (2026-05-13) — spec said "AFTER the transaction commits"; mechanism assumed an outer BEGIN/COMMIT that doesn't exist on the call chain (cycle-assembly runs in autocommit) → SCP-2026-05-13 (Path I) amendment.
4. **Story 7.7 AC1 cron call-chain trace** (2026-05-13) — spec required cross-customer SELECT; mechanism (cron pragma comment + pool-wide query) needed explicit trace through `dispatcher.js:189` + `monthly-partition-create.js:1-3` dual precedent → spec carried mechanism trace from sharding-time, caught zero in-pipeline.

**Decision (Q4 = b + caveat)**: Promote to `feedback_spec_mechanism_trace.md` memory rule **AND** make Mechanism Trace a required H3 sub-section under Dev Notes in `bmad-create-story`'s story template. Memory entry alone is not sufficient — without template codification the discipline drifts back to optional Dev Notes within a few epics and the 4-sighting pattern re-emerges. The Mechanism Trace sub-section is not optional and not at sharding-time Bob's discretion.

**Action**: Write memory entry + locate `bmad-create-story` template and add `### Mechanism trace` as a required H3 sub-section under Dev Notes. Owner: Pedro (template edit); Bob (memory entry). Sequence: memory entry lands with this retro; template edit lands before Story 8.1 sharding.

### A2. Sharding-time mechanism-trace empirical validation

| Story | Spec length | Mechanism trace section | Step 4 catches | Step 5 catches | New unit fails | /bad-review verdict |
|---|---|---|---|---|---|---|
| 7.4 | 542 lines | AC1 sub-block | 1 (hook ordering) | 2 (vacuous assert + CSRF comment) | 0 | clean |
| 7.5 | 770 lines | AC1 sub-block | 0 | 0 | 0 | clean (1 cosmetic body finding) |
| 7.7 | 604 lines | AC1 sub-block | 1 (stale-state assertion) | 1 (cents-vs-euros prod bug) | 0 | clean (4 deferred findings, all post-merge) |

**Discussion**: Mechanism-trace discipline correlates with clean BAD pipelines (zero new unit failures, all three verdicts clean). The 7.5 zero-catch result is consistent with the spec's literal-correctness — the mechanism trace was right the first time and dev followed it. The cost is spec length (~150 lines per story in the AC1 sub-block). Trade-off: AC1 contract bloats with descriptive context that isn't AC-grade scope.

**Decision (Q4 = b + caveat, paired with A1)**: Move mechanism trace from AC1 sub-block to required H3 sub-section under Dev Notes. Preserves the in-pipeline catch leverage (Step 4/5 subagents read Dev Notes per skill design), drops ~150 lines from AC1 contract per story. The template codification (per A1 caveat) is load-bearing here — without it, mechanism trace drifts to "optional Dev Notes thing" and the leverage is lost.

---

## B. Bundle B payload-shape gap

### B1. Reconciliation.js canonical shape vs cycle-assembly mis-shape

**Evidence**: `worker/src/safety/reconciliation.js` (Story 7.7, Pattern A) emits `tier-transition` with canonical `{ fromTier, toTier, reason }` payload (matches [`shared/audit/event-types.js:260-264`](../../shared/audit/event-types.js#L260-L264) `PayloadForTierTransition` typedef) and `new-competitor-entered` with `{ skuId, competitorPriceCents, cycleId }` — **first production module to emit canonical-shape payloads**. `worker/src/cycle-assembly.js:199-211` emit loop still passes a single decision-context payload (`{ action, newPriceCents, ... }`) to ALL events including `tier-transition`, regardless of slug. Side-by-side data point now exists: reconciliation got to ship correctly because it's a new code path; engine path inherited fixture-locked behavior.

**Decision (Q2 = c + caveat)**: Defer to Phase 2. Trigger is **Story 9.3 sharding** specifically — the first audit-feed surface that renders payload contents as customer-facing audit rows (atenção feed + notável feed + daily summary). Epic 8 dashboard cards (8-1, 8-2) likely only need event-type slug + counts (no payload field rendering); Story 8.7 anomaly-review consumes anomaly-freeze events which already ship canonical via Story 7.4 Pattern A. Story 9.3 is the structural trigger.

**Pre-flag for deferred-work.md**: amend the existing entry at `deferred-work.md:472` with: "Trigger = Story 9.3 sharding (5 surface query endpoints — atenção feed + notável feed + daily summary). Hard prerequisite: cycle-assembly payloadFor helper retrofit MUST land before Story 9.3 dispatches." This prevents Story 9.3 from shipping with wrong-shape payload renders.

### B2. Engine-vs-reconciliation semantic divergences (paired)

**Evidence (a)** — Steady-state T3 emission: engine path emits `tier-transition` on every T3 cycle visit (Story 7.5 AC#4 epic-vs-fixture lock-in per SCP Amendment 4 — fixture `p11-tier3-no-competitors._expected.auditEvent='tier-transition'` wins as sole oracle); reconciliation path (Story 7.7 AC3) emits ZERO on steady-state (observability-only warn-log — dispatcher predicate already covers drift). **Engine path emits noise; reconciliation is correct.**

**Evidence (b)** — Payload shape (B1 above): engine path mis-shape vs reconciliation canonical shape.

Both are the same family of issue: engine path inherited fixture-locked behavior, reconciliation shipped correctly because it's a new module unconstrained by pre-existing fixtures. The payload-shape retrofit (B1) closes (b); (a) is a separate AC#4 wording correction.

**Decision**: Pair with B1 — the cycle-assembly emit-loop retrofit (B1 fix path (a) from `deferred-work.md:472`) is the right place to also rationalise (a) — when the emit loop adopts `payloadFor(slug, context)`, it can simultaneously suppress steady-state T3 tier-transition emissions per Pattern A discipline. Both close at Story 9.3 prerequisite. Trigger language in deferred-work covers both.

---

## C. Bundle B atomicity (autocommit vs BEGIN/COMMIT)

### C1. Doc-vs-code contradiction in project-context.md §3

**Evidence**: `worker/src/cycle-assembly.js:106-217` per-SKU loop runs in autocommit-per-statement mode (no outer BEGIN/COMMIT wraps the `assembleCycle` call from `dispatcher.js:243`; only micro-tx around cycle-start/cycle-end audit events at `dispatcher.js:136-144`). SCP-2026-05-13 (Path I) ratified this for Story 7.4 + Story 7.5 + Story 7.7. project-context.md §3 still reads: "state mutation + audit emission in ONE caller-owned tx" — doc-vs-code contradiction.

**Decision (Q1 = b)**: Downgrade project-context.md §3 language to match shipped reality. Path (a) — wrap cycle-assembly's per-SKU loop in explicit BEGIN/COMMIT — builds plumbing for a failure mode (outer-tx rollback breaking state+audit pair) that **cannot currently manifest** because there is no outer tx to roll back. Per `feedback_no_premature_abstraction`: verify the problem has actually manifested before building. The Bundle B atomicity failure mode is hypothetical against the current production tx topology — exactly the imagined-shape reasoning Bob caught me proposing for Story 7.4 AC1. Path (c) — defer — leaves the doc-vs-code contradiction in place and creates the same documentation drift the 4-sighting mechanism-trace pattern just spent the session closing; future Bob shards would read §3 verbatim and propagate "single transaction" language into specs. Path (b) is the honest fix.

**Promotion trigger**: if autocommit semantics ever cause a real bug (audit row INSERTed but state UPDATE silently failed, or vice versa), promote to (a) — wrap in BEGIN/COMMIT.

**Action**: Edit `project-context.md` §3 to downgrade the language from "state mutation + audit emission in ONE caller-owned tx" to "sequenced autocommits with post-persistence side effects". Owner: Bob (this retro). Lands with this retro doc.

---

## D. Bundle C floor spec drift

### D1. Missing positive `anomaly-freeze` assertion

**Evidence**: Stories 7.4 + 7.5 specs forecasted Bundle C floor expansion to ≥48 (new positive `anomaly-freeze` assertion from Story 7.4). Reality: floor stayed at 47/47 — Story 7.4's assertion landed inside AC1's parametric loop (per-fixture switch on `_fixture_meta.expected.auditEvent`), not as a new `test()` block. `/bad-review` caught this on PR #95 (Bundle C floor stays 47, not 48). Story 7.7 spec corrected to ≥47. Captured at `deferred-work.md:481`.

**Decision**: Path (a) — ship the missing positive `anomaly-freeze` assertion as a new `test()` block. Closes the spec wording, +1 test count (Bundle C floor → 48). Cost is one trivial test mirroring the AC1 parametric-loop assertion but as a standalone block. Path (b) (amend spec to 47) leaves the spec wording inconsistent with the original Story 7.4 forecast.

**Action**: Add `anomaly-freeze` positive-assertion test() block to `tests/integration/full-cycle.test.js` (or appropriate Bundle C atomicity file). Land in a follow-up story or fold into pre-Story-8.1 housekeeping. Owner: Pedro (skill/dev choice).

---

## E. BAD pipeline tooling wins (validate + log)

### E1. Step 6 SOLO-PR DEFENSIVE TOUCH validated end-to-end

**Evidence**: Commit `99d3fea` on main (2026-05-13) auto-injects `Skip-Live-Smoke` marker for solo PRs touching `worker/src/safety|engine/` + `shared/mirakl/` with no Mirakl API surface tokens in diff. Validated on Story 7.7 PR #96 (2026-05-14): auto-inject fired correctly, no manual `gh api PATCH` workaround needed. 3-sighting pattern (Story 7.9 deferred-work `deferred-work.md:442` → Story 7.4 → Story 7.7 confirmed) **CLOSED**.

**Action**: Mark `deferred-work.md:442` as CLOSED — auto-inject extended to solo PRs and validated.

### E2. `.gitignore` exclusion for scratch files validated

**Evidence**: Commit `52e459a` on main (2026-05-13) added `pr*.diff` + `pr*-body.tmp.md` to repo-level `.gitignore`. Closed 5-sighting pattern (`pr73-body.tmp.md`, `pr76.diff`, `pr79.diff`, `pr93.diff`, `pr94.diff`) of scratch files blocking `git worktree remove`. Captured at `deferred-work.md:451`.

**Action**: Mark `deferred-work.md:451` as CLOSED.

### E3. `/bmad-correct-course` thrice-validated

**Evidence**: Memory `feedback_correct_course_validated_for_spec_failures` updated this session to record the third validation:
1. Story 6.3 wire-up (Bundle C 2026-05-09)
2. Story 7.8 fake-gate (Bundle C 2026-05-11)
3. Story 7.4 AC1 in-tx alert (post-close-out 2026-05-13)

The `/bmad-correct-course → /bad-review pass-2 → merge` loop is a documented mature recovery pattern. No retro action needed beyond the memory update already shipped.

### E4. Mechanism-trace discipline (folded into A2)

Discipline matured through Stories 7.4/7.5/7.7 with measurable in-pipeline catch correlation. Promoted to template requirement per A1 + A2.

---

## F. BAD pipeline tooling gaps (action items)

### F1. Push-race on Q4 done-flip gate (3-sighting → action item)

**Evidence**: 3 sightings now:
1. **Story 5.1** (Epic 5 retro 2026-05-08, fe95a9d) — first observation
2. **Story 7.4** (PR #94, 2026-05-13) — sprint-status flipped manually post-merge per `7-4` entry: "post-merge reconciliation after PR #94 squash-merge b7ab679; push race stranded BAD Step 7 final flip"
3. **Story 7.7** (PR #96, 2026-05-14) — same pattern: "post-merge reconciliation after push race stranded BAD Step 7 final flip"

Crosses 3-sighting threshold. Promoted from watch to action item.

**Decision (Q3 = b)**: Move done-flip from Step 7 to Phase 0 post-merge reconciliation. Phase 0 already runs and currently fixes the race manually — make it Phase 0's authoritative responsibility, not Step 7's. Eliminates the race by design (Step 7's write goes away, no concurrent-write window can exist). Path (a) retry-on-race doesn't fix the failure mode, just narrows it. Path (c) accepts manual edits as standing cost; with 3 sightings we know it'll recur — no reason to pay manual cost forever.

**Trade-off documented**: brief sprint-status incorrectness window between PR-merge and next Phase 0 (sprint-status shows "review" while origin/main has the merged squash). Acceptable — anyone consulting sprint-status between batches has git history as authoritative truth anyway.

**Action**: Edit `bad/SKILL.md` Step 7 to remove the done-flip block; edit `bad/SKILL.md` Phase 0 to add the done-flip logic (detect merged PRs since last reconciliation pass via `gh pr list --state merged --search "merged:>= <last_pass_ts>"` or similar; flip corresponding sprint-status keys `review → done`). Owner: Pedro (BAD upstream patch). Net line-budget impact must be ≤0 in `bad/SKILL.md`.

### F2. Bob's `/bmad-create-story` doesn't create worktree at shard time (3-sighting)

**Evidence**: 3 sightings (Stories 7.4, 7.5, 7.7) where BAD's Step 1 dispatched in "create or reuse" mode because Bob's `/bmad-create-story` doesn't create a worktree at shard time. BAD's skill-rule "ready-for-dev → skip Step 1" is misleading (Step 1 still fires to create the worktree).

**Decision**: Path (a) — Bob's `/bmad-create-story` creates the worktree at shard time (~10 lines in Bob's skill). Removes the conditional from BAD entirely. Cleaner separation: SM owns story spec + worktree; BAD owns ATDD + dev + review + PR.

**Action**: Edit `bmad-create-story/SKILL.md` to add worktree-creation block after sprint-status flip → ready-for-dev. Edit `bad/SKILL.md` Step 1 to assume worktree exists (no "create or reuse" branch). Owner: Pedro. Land before Story 8.1 sharding.

### F3. Sprint-status YAML entry verbosity

**Evidence**: Per-story annotations now run 1500+ chars on a single YAML line (Story 7.5 ~1600 chars, Story 7.7 ~1900 chars). Pure metadata accumulation; readability suffers when reviewing the file.

**Decision**: Path (b) — terser annotation discipline. Annotations become reference-pointers ("flipped X→Y 2026-05-NN; see commit ABC for details") not self-contained narratives. Full context lives in commit messages, story Dev Notes, and retro docs. Path (a) (multi-line YAML block) preserves verbosity in a different format; path (c) accepts increasing line lengths as audit trail (correct in spirit, but the audit trail is duplicated in 3+ places).

**Action**: Apply to new annotations going forward; do NOT retroactively edit existing Epic 7 entries (they're frozen audit trail). Owner: Bob (discipline going forward). Captured here as a working convention; no skill edit required.

### F4. Subagent C shallow-read trend

**Evidence**: 3-batch tool-use degradation:
| PR | Tool uses | Tokens |
|---|---|---|
| #94 (Story 7.4) | 21 | 109.8k |
| #95 (Story 7.5) | 11 | 64.0k |
| #96 (Story 7.7) | 5 | 18.4k |

Subagent C still produced useful findings on PR #96 (3 critical-path test gaps — J2 below), but depth may be insufficient. Possible causes: story-specific test surface size; subagent prompt drift; random variance.

**Decision**: Investigate Subagent C prompt for recent edits that may have reduced scope. If no edit found, classify as variance (story-specific test surface) and watch. Watch trigger: if next 3 bad-review runs (Epic 8 first 3 stories) all show <10 tool uses with <50k tokens, promote to P0 — Subagent C prompt audit.

**Action**: Pedro spot-checks `bad-review/references/subagents/code-vs-tests.md` (or whichever file is Subagent C) for recent edits. Capture finding here in follow-up.

---

## G. Documentation hygiene

### G1. Dev Agent Record empty pattern — 3-sighting promotion BUSTED

**Evidence**:
- Story 7.4 DAR: empty (`deferred-work.md:460`)
- Story 7.5 DAR: empty (`deferred-work.md:489`)
- Story 7.7 DAR: **fully populated** (Agent Model Used + Debug Log References + Completion Notes List all present — verified via grep of `_bmad-output/implementation-artifacts/7-7-worker-src-safety-reconciliation-js-tier-3-daily-pass-nightly-reconciliation.md:593`)

Promotion threshold (3-sighting) NOT crossed. Stays at 2-sighting watch.

**Decision**: Watch item. If Epic 8's first story (8.1) ships with empty DAR, that's the 3rd sighting and template-fix trigger (BAD Step 3 dev subagent MUST back-fill DAR sub-sections before flipping atdd-done → review, ~5 line addition to step3-develop.md). Until then, current discipline (Step 3 dev subagent fills DAR per task contract) is empirically holding — Story 7.7 dev correctly populated all sub-sections including a 6-item Implementation Plan and detailed test results.

**Action**: Monitor Epic 8 first story. No edit yet.

---

## H. Opus-gate validation (positive data points)

### H1. Story 7.7 Step 5 caught a production bug

**Evidence**: Commit `cddd323` (Step 5, 2026-05-13): "currentPosition was mixing cents (ownTotal) vs euros (P11 competitor total_price) — would route T3 winning rows to losing branch." Fixed via `toCents()` boundary wrap (mirrors `decide.js:164-169`).

This is the Opus-locked critical-path gate paying off concretely:
- Would NOT have been caught by unit tests (synthetic inputs were internally consistent — bug only manifests with real P11 euro responses crossing a cents-typed comparator)
- WOULD have caused silent miscategorisation in production (Tier 3 winning rows wrongly routed to losing branch → tier oscillation + audit pollution)
- Is exactly the class of bug that motivated `no-float-price` ESLint + `shared/money/index.js` SSoT discipline

Validates the critical-path Opus discipline.

**Decision**: Defer memory entry to the autonomous-BAD-cadence decision at Epic 8. The data point is logged here as evidence; if Epic 8 considers downgrading Step 5 to Sonnet for non-critical-path stories, this finding becomes the load-bearing counterargument.

### H2. Other Opus catches across batch

- Story 7.4 Step 4 hook-ordering bug (test-review caught test would never assert what it claimed)
- Story 7.4 Step 5 vacuous assert + CSRF comment (route claimed CSRF protection that didn't exist)
- Story 7.7 Step 4 stale-state assertion tightening

3 Step 4/5 catches in Story 7.4, 0 in Story 7.5, 2 in Story 7.7. All Opus-tier. None would have surfaced in unit tests.

**Decision**: Track. Folds into Section A2's mechanism-trace empirical-validation table.

---

## I. Epic-spec drift

### I1. Two epic-spec wording divergences

**Evidence**:
- (a) **Story 7.5 AC#4** epic wording "no audit event for steady-state" vs fixture `p11-tier3-no-competitors._expected.auditEvent='tier-transition'`. Fixture wins per SCP Amendment 4 (fixture _expected sole oracle). Epic wording is divergent.
- (b) **Story 7.7 AC#2** epic wording "forces a check this cycle to recover" vs reconciliation's observability-only mechanism (dispatcher predicate already covers drift; same-iteration P11 call IS the recovery). Story spec clarified via mechanism trace. Epic wording is divergent.

**Decision**: Accept (story spec captures correct mechanism; epic wording is just imprecise). Defer epic-spec correction to Phase 2 — epic-spec is locked planning artifact; small wording fixes don't warrant SCP overhead unless operational confusion surfaces. Capture here as known drift.

**Promotion trigger**: if any future story sharding (Epic 8 or later) reads the divergent epic wording and propagates the wrong mechanism into its spec, promote to `/bmad-correct-course` to amend the epic wording.

---

## J. PR #96 / Story 7.7-specific findings (from /bad-review pass)

### J1. SELECT WHERE clause omits `s.excluded_at IS NULL` predicate

**Evidence**: `worker/src/safety/reconciliation.js:81-108` `RECONCILIATION_SQL`. Spec AC1 explicitly listed `excluded_at IS NULL` in the SELECT predicate set (column lives on `skus`, JOINed table, so the fix is `AND s.excluded_at IS NULL`). Impl omits the predicate. AC4 test 7 asserts `frozen_for_anomaly_review` + `pending_import_id` but not `excluded_at` — green test count does not prove spec compliance. Captured at `deferred-work.md:497`.

**Decision**: Ship as one-line follow-up patch + extended test 7 assertion. Dormant in MVP (no SKUs currently flagged `excluded_at`), becomes a real bug the moment Phase-2 manual-exclusion ships. Cost is ~5 lines, no spec amendment needed, closes a latent bug pre-Phase-2.

**Action**: Owner Pedro (one-line patch in `worker/src/safety/reconciliation.js` + extend `tests/worker/safety/reconciliation.test.js` test 7). Land in pre-Story-8.1 housekeeping batch alongside J2 and D1.

### J2. 3 critical-path test gaps in reconciliation.test.js

**Evidence** (captured at `deferred-work.md:498-500`):
- (a) **Decrypt-failure skip branch** (`reconciliation.js:246-252`) — decryptFn throws → row skipped, warn-logged, `last_checked_at` NOT bumped. No test pins behavior.
- (b) **P11 fetch-failure skip branch** (`reconciliation.js:268-274`) — silent skip on P11 outage. Production-safety invariant, not asserted.
- (c) **`worker/src/jobs/reconciliation.js` cron entry entirely untested** — defensive `.catch()` prevents `unhandledRejection` from destabilising the worker; cron schedule `'0 0 * * *'` + `{ timezone: 'Europe/Lisbon' }` also not pinned.

Critical-path coverage debt.

**Decision**: Ship as `tests/worker/safety/reconciliation.additional.test.js` supplement (and `tests/worker/jobs/reconciliation.test.js` for the cron entry). Land before Story 8.1 dispatches — critical-path safety coverage gaps don't carry into a new epic.

**Action**: Owner Pedro (new test files following the patterns described in deferred-work.md:498-500). Sequence after J1.

---

## K. Smoke-learned items (this session, 2026-05-14)

### K1. Worker fail-fasts on missing RESEND_API_KEY at module load

**Evidence**: `shared/resend/client.js:28` fails fast at module load. Reconciliation / cron-registration paths don't actually need Resend (per Story 7.7 Debug Log: "Task 4: RESEND_API_KEY env-stub NOT required (reconciliation.js has no transitive resend dependency — confirmed by grep through writer.js, event-types.js, envelope.js, money/index.js, logger.js import chains)"). The fail-fast is over-aggressive — blocks worker boot in dev environments without the full alert pipeline.

**Decision**: Phase 2 trigger — lazy Resend client instantiation. Defer `new Resend(KEY)` to first send-call, not module load. Short-term workaround: dev `.env` stubs RESEND_API_KEY with dummy value. Don't fix now; not blocking any current story.

**Action**: Capture in deferred-work.md as Phase 2 housekeeping. No retro action item.

### K2. `monthly-partition-create` cron doesn't log registration at boot

**Evidence**: Registered via inline `cron.schedule` (not via factory). Inconsistency with the other 4 crons (heartbeat, master-cron, pri02-poll, reconciliation) all of which log "X cron registered" via their factory. Ops can't visually confirm registration during boot.

**Decision**: Small refactor at Story 9.1 housekeeping — extract to `startMonthlyPartitionCron` factory mirroring the other three. Story 9.1 already touches `monthly-partition-create.js` (per AD-X), so it's the natural home.

**Action**: Note as Story 9.1 housekeeping in deferred-work.md. No standalone retro action.

### K3. bad-review TL;DR vocabulary standardisation

**Evidence**: bad-review TL;DR phrasing about "5-minute cron" conflated scheduler tick (the 5-min wake-up) with per-SKU scan frequency (15/45/1440 min cadence). Pedro flagged it.

**Decision**: Standardise TL;DR vocabulary in bad-review prompt:
- "scheduler tick" = the 5-min wake-up
- "per-SKU cadence" = 15/45/1440 min tier-specific re-check intervals

**Action**: Edit `bad-review/SKILL.md` (or relevant subagent prompt) to add an explicit clarification example in the TL;DR section. ~3-5 line addition. Owner: Pedro. Net line-budget impact comfortable (bad-review/SKILL.md at 678/750).

---

## L. Bundle C close-out follow-through

Systematic status check on the 9 action items (Q1–Q9) + 8 watch items (W1–W8) logged at the [Bundle C close-out retro 2026-05-13](bundle-c-close-out-retro-2026-05-13.md).

### L1. Action items Q1–Q9

| Item | Status at Epic 7 retro | Evidence |
|---|---|---|
| Q1 — bundle_dispatch_orders codification + Phase 0 enforcement | ✅ CLOSED | sprint-status.yaml `bundle_dispatch_orders:` block holds Bundle C order. All post-close-out stories (7.4, 7.5, 7.7) were solo dispatch — no parallel-dispatch errors. |
| Q2 — Phase 5.5a migration application verification | ⏸️ NOT EXERCISED | Stories 7.4-7.7 ship zero migrations. Verification mechanism untested since close-out. Carry forward to Epic 8 or whenever migrations resume. |
| Q3 — PR body remove "Files Changed" line | ✅ CLOSED | `bad/references/subagents/step6-pr-ci.md` explicitly prohibits the line. No "Files Changed" lines in PRs #94/#95/#96 bodies. |
| Q4 — subagent guardrail-override discipline (halt-and-ask) | ✅ CLOSED | Step 6 FORCE-PUSH EXCEPTION codified. No force-pushes attempted across 7.4/7.5/7.7. SCP-2026-05-13 followed the discipline (Bob halted to coordinator with proposed mechanism rather than overriding). |
| Q5 — bad-review Phase 2 inline-fallback banner | ⏸️ TBD | No inline-/bad-review dispatches happened in 7.4/7.5/7.7 (all ran top-level per [S] pick per `feedback_nested_subagent_dispatch_limit`). Banner readiness untested in practice. **Decision**: accept that top-level discipline made the banner moot for now; verify banner exists in `bad-review/SKILL.md` Phase 2 only if a future inline-fallback dispatch becomes necessary. |
| Q6 — bad-review banner-on-stale MCP | ⏸️ PARTIALLY VALIDATED | `/mcp authenticate` mid-/bad-review exercised on PRs #94 + #95 (recall "MCP available (X data tools)" lines in audit Phase 1 output). Stale-MCP scenario (MCP unavailable BEFORE Phase 1) not exercised. **Decision**: defer to next bad-review with token already expired; verify whether banner fires or audit proceeds silently with training-data fallback. |
| Q7 — Bundle-C-cleanup chore PR | ✅ CLOSED | Story 7.9 shipped via PR #93 2026-05-13. 9 cleanup items bundled. |
| Q8 — CI two-phase rollout | ⏸️ Phase 1 CLOSED, Phase 2 OPEN | `.github/workflows/ci.yml` gates 3 Bundle C integration tests per post-`1501c72` narrowing (`deferred-work.md:450`). Phase 2 (full `npm test` in CI) PENDING — requires isolating the hanging non-Bundle-C integration files (key-entry suspected per `project_key_entry_test_notes`). Carry forward as pre-Go-Live stabilisation item. |
| Q9 — synthetic integrated-branch recovery memory entry | ✅ CLOSED | Memory `reference_synthetic_integrated_branch_recovery` exists per MEMORY.md. |

**L1 disposition**: 5 CLOSED (Q1, Q3, Q4, Q7, Q9), 2 TBD (Q5, Q6), 1 deferred (Q2), 1 split (Q8 Phase 1 closed / Phase 2 open).

### L2. Watch items W1–W8

| Item | Status at Epic 7 retro | Disposition |
|---|---|---|
| W1 — bundle dispatch enforcement on next bundle | ⏸️ NOT EXERCISED | No Bundle D dispatched yet. Defer to Phase 2 when Story 12.1 / Stories 11.5+10.3 dispatch. |
| W2 — 28/546 unit fails baseline | ⏸️ Bundle-C-portion CLOSED | Story 7.9 closed the 5 Bundle-C-introduced fails. 23 pre-existing fails confirmed CONSISTENT across 7.4/7.5/7.7 runs (~571 pass / 23 fail baseline). 23 pre-existing carries forward as pre-Go-Live stabilisation item. |
| W3 — Worten scan worker hardening | ⏸️ Epic 4 carry-forward | Not exercised. Defer to Epic 4 housekeeping or whenever scan worker is next touched. |
| W4 — scan.js bare fetch | ⏸️ Epic 4 carry-forward | Same disposition as W3. |
| W5 — pri01_staging RLS USING gap | ⏸️ NOT EXERCISED | Not touched in Stories 7.4-7.7. Defer to whenever pri01_staging RLS is next examined (likely Epic 8 dashboard work consuming pri01_staging for status display). |
| W6 — cooperative-absorb anomaly-freeze stub-fallback | ✅ RESOLVED | Story 7.4 (SCP-2026-05-13 + Amendment 7) dismantled the stub-fallback. Production code now static-imports `freezeSkuForReview` from `worker/src/safety/anomaly-freeze.js`. |
| W7 — bypass-aware middleware in margin.js | ⏸️ Epic 4 carry-forward | Same disposition as W3. |
| W8 — max_discount_pct sentinel + key.js placeholder | ⏸️ Epic 4 carry-forward | Same disposition as W3. |

**L2 disposition**: 1 RESOLVED (W6), 1 partial-close (W2 Bundle-C-portion), 4 Epic 4 carry-forwards (W3/W4/W7/W8), 2 deferred (W1, W5).

### L3. Aggregate disposition

**CLOSED at Epic 7 retro**: Q1, Q3, Q4, Q7, Q9, W2-(Bundle-C-portion), W6 = **7 items**
**PARTIALLY CLOSED**: Q8 Phase 1 (Phase 2 OPEN) = **1 item**
**TBD / VERIFY**: Q5, Q6 = **2 items**
**DEFERRED to Phase 2**: Q2, W1, W5 = **3 items**
**EPIC 4 CARRY-FORWARD**: W3, W4, W7, W8 = **4 items**

17-item Bundle C close-out follow-through: 7 clean closures + 2 verifications + 3 forward deferrals + 4 cross-epic carry-forwards.

### L4. New evidence about Bundle C participants discovered POST-close-out

**L4.a — Payload-shape mismatch** (covered in B1 above): affects ALL Bundle C participants emitting through cycle-assembly (5.2, 7.2, 7.3, 7.6) plus post-close-out 7.4 + 7.5 (Pattern C). Reconciliation (7.7, Pattern A) ships canonical shape, providing the side-by-side data point. **Bundle C close-out retro overlooked this — production-correctness verdict was based on event-type slug + emission timing, not payload-shape conformance.** Story 9.3 prerequisite (per B1 caveat) closes the loop.

**L4.b — Bundle B autocommit-vs-BEGIN/COMMIT** (covered in C1 above): close-out retro ratified the autocommit semantic via SCP-2026-05-13 for Story 7.4 AC1 specifically. Decision now extends project-wide via project-context.md §3 downgrade (per Q1=b).

---

## Previous retro follow-through

Epic 6's BAD-pipeline retro (2026-05-10) logged 5 action items (Q1 P0 + Q2/Q3/Q4/Q5 strongly recommended) + 5 watch items. All Q1-Q5 action items dispositions were already captured at the Bundle C close-out retro 2026-05-13 (which absorbed Epic 6 retro Q5 into close-out Q5+Q6, and validated Q1+Q3+Q4 across Bundle C dispatches). Q2 (ground-truth verifier) was DEFERRED at close-out (low verification rate not promoted to P0 because Bundle C live-fire produced higher S/N).

This retro confirms:
- **Epic 6 Q1** (Step 7 sub-step completeness): held cleanly across 7.4/7.5/7.7 — no truncation incidents.
- **Epic 6 Q2** (ground-truth verifier): NOT promoted; verification rates on 7.4/7.5/7.7 were materially better than Story 6.3's 1.6% (estimated 30-50% based on triage notes). Watch trigger preserved for Epic 8.
- **Epic 6 Q3** (spec-wire-up [WIRE-UP] layer): NOT exercised in 7.4/7.5/7.7 (no [WIRE-UP] markers used). Pattern matured into Mechanism Trace discipline (A1) which captures cross-file invariants more comprehensively. **Decision**: Epic 6 Q3 superseded by A1 — close as superseded.
- **Epic 6 Q4** (capture /bmad-correct-course pattern): captured via `feedback_correct_course_validated_for_spec_failures` memory entry (now thrice-validated per E3).
- **Epic 6 Q5** (MCP availability pre-check + fallback banner): absorbed into Bundle C close-out Q5+Q6; status per L1 (Q5 TBD, Q6 partially validated).

**Epic 6 retro disposition**: all 5 action items either CLOSED (Q1, Q4), superseded (Q3 → A1), or rolled into close-out (Q5). Q2 remains as a deferred watch.

---

## Significant Discoveries

| Check | Result |
|---|---|
| Architectural assumptions from planning proven wrong? | Yes — Bundle B atomicity language in project-context.md §3 doesn't match shipped reality. Resolved by Q1=b (downgrade language). |
| Major scope changes or descoping during Epic 7? | No — all 9 stories shipped as planned. SCP-2026-05-13 was a mechanism amendment, not a scope change. |
| Technical approach needs fundamental change for Epic 8? | No — Epic 8's dashboard surface contracts are unchanged. **One soft prerequisite**: cycle-assembly payloadFor helper retrofit before Story 9.3 (per B1 caveat). |
| Dependencies discovered that Epic 8 doesn't account for? | No new ones. CSRF wire-up dependency for Story 8.7 (anomaly-review modal) was already known (`deferred-work.md:468`). |
| New BAD pipeline failure modes uncovered? | One: push-race on Q4 done-flip (F1, 3-sighting). Addressed by Q3=b (move to Phase 0). |
| Memory promotion warranted? | Yes — promote `feedback_spec_mechanism_trace` (4-sighting) per A1. |
| Architecture-distillate drift? | No drift detected; context-budget audit clean. |

**Verdict**: NO Epic 8 epic-spec changes required. One sequenced architectural amendment (project-context.md §3 downgrade per C1), one template change (story-template.md Mechanism Trace per A1+A2), one BAD coordinator patch (Phase 0 done-flip per F1), one Bob skill patch (`/bmad-create-story` worktree per F2). Before Story 8.1 sharding: J1 + J2 + D1 ship as pre-Epic-8 housekeeping batch.

---

## Action Items

### P0 — Lands with this retro

**A1 + A2 — Mechanism Trace promotion**
- Write memory entry `feedback_spec_mechanism_trace.md` documenting the 4-sighting pattern + the H3 sub-section requirement.
- Owner: Bob.
- Lands: this retro.

**C1 — project-context.md §3 downgrade**
- Edit `project-context.md` §3 to replace "state mutation + audit emission in ONE caller-owned tx" with "sequenced autocommits with post-persistence side effects" semantic. Preserve Bundle B's existence as a structural concept (it still describes the pre-commit + post-commit emission ordering invariant).
- Owner: Bob.
- Lands: this retro.

**E1 + E2 + Q9 + W6 + memory closures (deferred-work.md)**
- Mark `deferred-work.md:442` (E1 solo-PR auto-inject), `deferred-work.md:451` (E2 .gitignore), `deferred-work.md:466` (C1 Bundle B atomicity — resolved via Q1=b), `deferred-work.md:467` (A1 mechanism trace — resolved via memory + template) as CLOSED.
- Amend `deferred-work.md:472` (B1 payload-shape) with explicit Story 9.3 trigger language.
- Owner: Bob.
- Lands: this retro.

### Pre-Story-8.1 housekeeping batch (single mini-batch before Bob shards 8.1)

**A1 — Template change: required Mechanism Trace H3 sub-section**
- Locate `bmad-create-story`'s story template (likely `.claude/skills/bmad-create-story/templates/story-template.md` or `_bmad/bmm/templates/story-template.md` — find via Glob).
- Add `### Mechanism trace` as a required H3 sub-section under Dev Notes. Document the required content (call-chain trace + tx-topology citation + commit-boundary explicit + statement-ordering justification).
- Owner: Pedro.
- Sequence: before Bob shards Story 8.1.

**F1 — BAD Step 7 done-flip → Phase 0 migration**
- Edit `bad/SKILL.md` Step 7 to remove the done-flip block.
- Edit `bad/SKILL.md` Phase 0 to add merged-PR detection + sprint-status flip logic.
- Net line-budget impact ≤0 (compress equivalent prose elsewhere in `bad/SKILL.md`; currently 968/1000).
- Owner: Pedro.
- Sequence: before next BAD dispatch (Story 8.1).

**F2 — Bob's `/bmad-create-story` creates worktree at shard time**
- Edit `bmad-create-story/SKILL.md` to add worktree-creation block after sprint-status flip → ready-for-dev.
- Edit `bad/SKILL.md` Step 1 to assume worktree exists (remove "create or reuse" conditional).
- Owner: Pedro.
- Sequence: paired with F1.

**D1 — Ship missing positive `anomaly-freeze` assertion**
- Add a new `test()` block to `tests/integration/full-cycle.test.js` asserting positive anomaly-freeze emission. Bundle C floor expands to ≥48.
- Owner: Pedro.
- Sequence: in pre-Epic-8 housekeeping batch.

**J1 — `s.excluded_at IS NULL` predicate in reconciliation.js**
- Add `AND s.excluded_at IS NULL` to `RECONCILIATION_SQL` (`worker/src/safety/reconciliation.js:81-108`).
- Extend `tests/worker/safety/reconciliation.test.js` test 7 to assert the predicate.
- Owner: Pedro.
- Sequence: paired with J2.

**J2 — Reconciliation critical-path test gaps**
- Create `tests/worker/safety/reconciliation.additional.test.js` covering decrypt-failure skip + P11 fetch-failure skip per `deferred-work.md:498-499`.
- Create `tests/worker/jobs/reconciliation.test.js` covering cron entry defensive `.catch()` + schedule + TZ per `deferred-work.md:500`.
- Owner: Pedro.
- Sequence: paired with J1.

**K3 — bad-review TL;DR vocabulary standardisation**
- Edit `bad-review/SKILL.md` TL;DR section to add explicit "scheduler tick" vs "per-SKU cadence" clarification example.
- Net line-budget impact: ~3-5 lines added; comfortable (678/750).
- Owner: Pedro.
- Sequence: in pre-Epic-8 housekeeping batch.

### Watch items

**W-F4 — Subagent C shallow-read trend**
- Pedro spot-checks Subagent C prompt for recent edits that may have reduced scope.
- Watch trigger: if next 3 bad-review runs (Epic 8 first 3 stories) all show <10 tool uses with <50k tokens, promote to P0 — Subagent C prompt audit.
- Owner: Pedro.

**W-G1 — Dev Agent Record empty pattern (2-sighting watch)**
- If Epic 8's first story (8.1) ships with empty DAR, that's the 3rd sighting → template-fix trigger (BAD Step 3 dev subagent backfills DAR sub-sections, ~5 line addition to step3-develop.md).
- Owner: Bob to flag at next dispatch review.

**W-I1 — Epic-spec wording divergences**
- Two known divergences (Story 7.5 AC#4 + Story 7.7 AC#2) accepted as locked-doc imprecision. Promote to `/bmad-correct-course` only if a future story shard propagates the wrong mechanism into its spec.
- Owner: Bob.

**W-K1 — Resend client lazy instantiation**
- Phase 2 housekeeping. Promote if worker boot fails-fast on a dev environment without RESEND_API_KEY blocks substantive work.
- Owner: Pedro.

**W-K2 — `monthly-partition-create` cron logging**
- Story 9.1 housekeeping. Extract to `startMonthlyPartitionCron` factory mirroring the other three crons.
- Owner: Bob (flag at Story 9.1 sharding time).

### Deferred to Phase 2

**B1 — Cycle-assembly payloadFor helper retrofit**
- Trigger: **Story 9.3 sharding** (5 surface query endpoints — atenção feed + notável feed + daily summary). HARD PREREQUISITE: cycle-assembly payloadFor helper retrofit MUST land before Story 9.3 dispatches.
- Captured at `deferred-work.md:472` (amended this retro with Story 9.3 trigger language).
- Owner: Bob to flag at Story 9.3 sharding.

**C1 follow-up — Bundle B real BEGIN/COMMIT atomicity**
- Promote only if autocommit semantics cause a real bug (audit row INSERTed but state UPDATE silently failed, or vice versa).
- Watch indefinitely.
- Owner: Pedro.

**Q2 (close-out) — Phase 5.5a migration application verification**
- Trigger: next time a migration ships (likely Epic 8 dashboard work or Story 9.x audit-feed surfaces).
- Owner: Pedro.

**Q5 (close-out) — bad-review Phase 2 inline-fallback banner**
- Trigger: next inline-/bad-review dispatch (currently moot per [S] top-level discipline).
- Owner: Pedro.

**Q6 (close-out) — bad-review banner-on-stale MCP**
- Trigger: next bad-review with token already expired before Phase 1.
- Owner: Pedro.

**Q8 Phase 2 (close-out) — Full `npm test` in CI**
- Trigger: pre-Go-Live stabilisation. Isolate hanging non-Bundle-C integration files first (key-entry suspected per `project_key_entry_test_notes`).
- Owner: Pedro.

**W1/W5 (close-out) — Bundle dispatch enforcement + pri01_staging RLS USING gap**
- Trigger: respective next-touch events.
- Owner: Bob to flag at sharding.

### Epic 4 carry-forwards (W3, W4, W7, W8)

All four items defer to Epic 4 housekeeping or whenever the relevant route/scan code is next touched. No standalone action this retro.

---

## Readiness Assessment

| Area | Status | Notes |
|---|---|---|
| Epic 7 stories shipped | ✅ 9/9 | All merged to main. |
| Production-correctness verdict | ✅ Closed | Bundle C close-out retro (2026-05-13) + post-close-out stories 7.4/7.5/7.7 ATDD-driven without /bad-review escapes. |
| Live-smoke verdict | ✅ Closed | Story 7.7 PR #96 boot-smoke (2026-05-14): worker boots, all 4 crons register (heartbeat + master-cron + pri02-poll + reconciliation), reconciliation log line verbatim per AC2. |
| Joint-correctness verdict | ✅ Closed | 45/45 Bundle C integration tests pass (gated by `.github/workflows/ci.yml` Phase 1 since `1501c72`). |
| Bundle B atomicity contract | ✅ Reconciled | project-context.md §3 downgraded to match autocommit-per-statement reality (Q1=b). |
| Payload-shape conformance | ⏸️ Deferred | Reconciliation ships canonical; cycle-assembly emit loop still mis-shaped. Story 9.3 prerequisite. |
| Mechanism Trace template discipline | ⏸️ Action pending | Memory entry lands this retro; template change pre-Story-8.1. |
| BAD push-race | ⏸️ Action pending | F1 patch pre-Story-8.1. |
| Pre-Story-8.1 housekeeping batch | ⏸️ Pending | 7 items (D1, F1, F2, J1, J2, K3, template-change). |
| Context budget | ✅ Clean | TOTAL 34.5k tokens, all files within threshold. |
| Sprint-status retrospective key | ✅ Flipped | `epic-7-retrospective: optional → done` (this retro). |
| Sprint-status epic key | ✅ Flipped | `epic-7: in-progress → done`. |

---

## Key Takeaways

1. **Mechanism-trace discipline correlates with clean pipelines, but the trace belongs in Dev Notes, not AC1 contract.** Three post-close-out stories (7.4/7.5/7.7) shipped with zero new unit failures and clean /bad-review verdicts. The cost (spec length 542-770 lines) lives in AC1 sub-blocks today; moving to a required H3 sub-section under Dev Notes preserves catch leverage while keeping AC1 contract-grade. Template codification is load-bearing — without it the discipline drifts back to optional and the 4-sighting pattern re-emerges.

2. **Doc-vs-code contradictions are the same failure-mode family as spec-outcome-without-mechanism.** Bundle B atomicity language in project-context.md §3 promised "single transaction" while shipped code runs autocommit-per-statement. Same shape as Story 7.4 AC1 — outcome described, mechanism non-existent. Resolution per `feedback_no_premature_abstraction`: match docs to reality (Q1=b), promote to (a) wrap-in-tx only if the imagined failure mode actually manifests.

3. **Reconciliation as the first canonical-payload emit site exposes pre-existing engine-path drift.** cycle-assembly's mis-shaped payload was invisible until a side-by-side correct implementation (Story 7.7 Pattern A) shipped. Story 9.3 is the structural trigger — it's the first audit-feed surface that renders payload contents to customers. Pre-flagging the prerequisite in deferred-work.md prevents Story 9.3 from inheriting the gap.

4. **3-sighting promotion threshold caught the push-race; busted the empty-DAR pattern.** F1 crosses (5.1 + 7.4 + 7.7); G1 doesn't (7.4 + 7.5 + 7.7-populated). The 3-sighting rule does its job both ways — promote when the pattern is real, hold when the empirical data contradicts the prediction.

5. **The session-spanning Bundle C close-out → post-close-out → Epic 7 retro arc validates the multi-retro pattern.** Bundle C close-out (2026-05-13) closed joint-correctness for the mega-merge; this Epic 7 retro picks up the 3 post-close-out stories + 17-item follow-through. The close-out retro's verdicts held under Story 7.4/7.5/7.7 live conditions; only L4.a (payload-shape oversight) was missed by close-out and surfaced here. Sequencing retros at natural milestones (bundle-merge, epic-completion) compounds: each retro inherits the prior's verdicts and only adds new evidence.

6. **`/bmad-correct-course` is now the canonical mid-pipeline recovery tool.** Thrice-validated (Story 6.3 wire-up + Story 7.8 fake-gate + Story 7.4 AC1 in-tx alert). Memory entry `feedback_correct_course_validated_for_spec_failures` updated this session. The `/bmad-correct-course → /bad-review pass-2 → merge` loop is documented mature recovery — Bob owns the call per `feedback_bmad_sm_owns_spec_failures`.

---

## Next Steps (ordered)

1. **This retro doc artifacts** — write memory entry `feedback_spec_mechanism_trace`, edit project-context.md §3, mark 4 deferred-work entries CLOSED, amend deferred-work.md:472 with Story 9.3 trigger, flip sprint-status keys. Lands with this retro.

2. **Pre-Story-8.1 housekeeping batch** (7 items, Pedro's mini-batch before Epic 8 kickoff):
   - D1 anomaly-freeze positive assertion (Bundle C floor → 48)
   - F1 BAD Step 7 done-flip → Phase 0 migration
   - F2 `/bmad-create-story` worktree at shard time
   - J1 `s.excluded_at` predicate + extended test 7
   - J2 reconciliation .additional.test.js + jobs/reconciliation.test.js
   - K3 bad-review TL;DR vocabulary standardisation
   - A1 template change: required Mechanism Trace H3 sub-section in story-template.md

3. **Bob shards Story 8.1** — only after housekeeping batch lands. First Epic 8 story validates: (a) F1 Phase 0 done-flip works; (b) F2 SM-creates-worktree separation works; (c) A1 template change holds in practice; (d) G1 watch trigger fires or doesn't.

4. **Epic 8 retro** — full epic retro after Epic 8 closes. Validates A1 template discipline across a fresh epic, verifies F1+F2 BAD pipeline patches, and closes any remaining TBD items (Q5/Q6 from L1).

5. **Story 9.3 sharding (Phase 2)** — Bob flags B1 prerequisite at shard time; cycle-assembly payloadFor retrofit MUST land before dispatch.

---

**Epic 7 retrospective complete.** Sprint-status updates: `epic-7-retrospective: optional → done`, `epic-7: in-progress → done`. Document logged at `_bmad-output/implementation-artifacts/epic-7-retro-2026-05-14.md`. 1 new memory entry promoted (`feedback_spec_mechanism_trace`). 4 deferred-work entries CLOSED. 1 deferred-work entry amended with Story 9.3 trigger language. project-context.md §3 downgraded to match shipped reality. 7 items in pre-Story-8.1 housekeeping batch await Pedro's action.
